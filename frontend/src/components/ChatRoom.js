import React from "react";

const ChatRoom = ({ chatroomId }) => {
  return (
    <div>
      <h2>Chat Room {chatroomId}</h2>
      {/* Sem môžeme pridať logiku na zobrazenie chatovacej miestnosti */}
    </div>
  );
};

export default ChatRoom;
